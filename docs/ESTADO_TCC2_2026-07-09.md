# ESTADO DO TCC2 — fechamento de 2026-07-09

Substitui `PLANO_CURTO_TCC2_handoff.md`, cujas tarefas foram **todas executadas**.
Tudo abaixo foi verificado por saída de comando rodada no GCP. Nenhum número foi inventado.

## Artefato de entrega
`G:\Temp\Template-Relatório-Técnico-BES-UTFPR\ModeloTCC.pdf` — **50 páginas**, 727.938 bytes,
compilado em `texlive/texlive:latest` (TeX Live 2026) na VM `tv-tex3-20260709-174520`.
Backup do manuscrito pré-faxina: `G:\Temp\backup-manuscrito-20260709-170513.zip`.

> São 50 páginas, e não as 49 anteriores, porque o Apêndice A cresceu: a saída real da CLI
> traz o envelope `results[]` + `summary` agregado, que a versão antiga não tinha.

## Runs de verificação no GCS (`gs://terravault-ml-artifacts/runs/`)
| RUN | O que provou |
|---|---|
| `verify-20260709-170102` | pytest, `make evaluate`, CLI do Apêndice A, modelo `v20260708_015533` |
| `verify2-20260709-173445` | 137/137 pytest, pylint 10,00, flake8 0, mypy 0, cobertura 76,92% |
| `verify3-20260709-174356` | bandit 0 achados, `quality_gate` overall **PASS** |
| `tex3-20260709-174520` | compilação final + sonda de isolamento da folha de rosto |

Todas as VMs foram deletadas. `terravault-prod` não foi tocada.

## O que reproduziu EXATAMENTE (nada mudou no texto)
- Detecção: TerraVault 100/100/100 · Checkov 100/95,7/97,8 · tfsec 100/87,0/93,0 · Terrascan 100/47,8/64,7
- Achados brutos: 23 / 187 / 107 / 63
- Híbrido: regras 50,0/16,7 · ML 48,7/45,5 · final 49,1/27,7 · separações 33,3 / 3,2 / 21,4
- Corpus do modelo: 10.639 módulos + 30.303 blobs → 35.294 vetores → 35.594 amostras
- `137` casos de teste

## O que DIVERGIU e foi corrigido no texto (nunca o contrário)
1. **Resumo e abstract diziam 21,7** pontos de separação. Real: **21,4**
   (`metrics.json`: 49,12 − 27,67). Corrigido em `ModeloTCC.tex`.
   *Este erro não estava no plano antigo — passou despercebido.*
2. **Cobertura 76,80% → 76,92%** (1720 statements, 397 não cobertos).
   Corrigido em `Avaliacao.tex:9` e `Conclusoes.tex:5`. Confirmado por `gate-metrics.json`.
3. **Apêndice A** (era de 02/07, anterior ao re-treino de 07/07):
   `score` 84 → **89** · `ml_score` 62,27 → **74,82** · `scan_time` 0,066 → 0,041 s
   · estrutura passou a ter `results[]`/`summary`/`file_size_kb`.
   `confidence` segue `MEDIUM`; seguem 12 vulnerabilidades. `ml_score` válido (≠ −1).
   O score é **truncado**, não arredondado (`scanner.py:132` usa `int()`): 0,6·100 + 0,4·74,8177 = 89,93 → 89.
4. **Tempos** substituídos pelos do GCP, por decisão sua, com o ambiente declarado no texto:
   0,58 / 150,72 / 35,73 / 146,89 s em `e2-standard-4`, Ubuntu 24.04.

## Tabela 5 (`tab:comparativo`)
Convertida para `tabularx` na largura do texto, colunas *ragged-right* com hifenização proibida
(`\newcolumntype{P}`/`{L}` + `\hyphenpenalty=\exhyphenpenalty=10000`, em `ModeloTCC.tex`).
Rótulos encurtados; a célula do FastAPI virou `(FastAPI, Prometheus, Grafana)` — com
`Prometheus/Grafana` ela estourava 16,6pt (o próprio log mostrava `Pro-metheus`).

**Overfull hbox: 1**, de 2,61105pt, na **folha de rosto da classe** (`\folhaderosto`).
Não é regressão: uma sonda com o mesmo preâmbulo *sem* `tabularx`, contendo só `\capa`+`\folhaderosto`,
produz o **mesmo** overfull. Ele só aparece no TeX Live 2026; o seu log original
(TeX Live 2022/Debian) tinha 0. Nenhuma caixa ruim na Tabela 5.
Erros TeX: **0**. Referências/citações indefinidas: **0**.

> Aviso do BibTeX `didn't find a database entry for "abnt-etal-list=99"`: pré-existente,
> vem do `abntcite` interpretando opção de pacote como chave. Inofensivo.

## Faxina (a pasta do manuscrito NÃO é repositório git — nada foi apagado)
Movidos para `_descartado/`: `Desenvolvimento.tex` (morto), `Diretrizes TCC*.md`,
`TERRAVAULT-SCANNER_HIBRIDO_DE_SEGURANCA.pdf` (duplicata byte-idêntica, agora desatualizada), `tmp/`.
Movidas para `Imagens/_unused/`: as 8 imagens órfãs. Restam só os 4 PDFs usados.
Artefatos de build removidos. Um único PDF na raiz.

## ⚠️ PENDENTE — alterações de código NÃO COMMITADAS
```
tests/test_api.py       # limiar 30 → 35 (era expectativa anterior ao re-treino)
requirements-dev.txt    # + bandit==1.9.4
```
**`bandit` era invocado por `scripts/quality_gate.py` mas não constava em nenhum `requirements`.**
Num checkout limpo o gate falhava com `[FAIL] Findings detected at -ll severity` — que era, na
verdade, módulo ausente disfarçado de achado de segurança. Com ele instalado: 0 achados, gate verde.

Commitar antes de trocar de máquina:
```bash
cd G:/Workspace/oguarni/terravault
git add tests/test_api.py requirements-dev.txt
git commit -m "fix(quality): declare bandit dep and retune stale secure-score bound"
git push
```

## Lixo local no repo (não versionado, mas pesado)
`docs/venv/` (1,5 GB) e `terravault/.venv/` (592 MB, dentro do pacote Python).

## Ainda fora do escopo
`Termo de Aprovação` segue comentado em `ModeloTCC.tex` — entra na versão final,
até 10 dias após a defesa (Art. 21 §2º). `TermoAprovacao.pdf` já está na pasta.
