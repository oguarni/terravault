# Core dependencies
python-hcl2==4.3.2
scikit-learn==1.6.1
numpy==1.26.4
joblib==1.4.2

# API and web framework
# 0.133.0 is the first release to drop the `starlette<1.0.0` cap (0.131.0 and
# 0.132.x pinned `starlette<1.0.0,>=0.40.0`). That cap, not starlette itself,
# is why the image shipped a vulnerable 0.52.1 — see the starlette note below.
# Minimal bump on purpose: 0.133.0 still asks only for pydantic>=2.7.0, which
# the pinned 2.10.6 satisfies, so nothing else in the tree moves.
fastapi==0.133.0
# Pinned explicitly even though it is fastapi's transitive ASGI core, because
# the version floor here is a security boundary and must be reproducible
# rather than "whatever pip resolved that day". 1.3.1 is the lowest release
# fixing both CVE-2026-48818 (SSRF and NTLM credential theft via UNC paths in
# StaticFiles) and CVE-2026-54283 (request.form() silently ignoring
# max_fields / max_part_size). Both were flagged independently by pip-audit
# and by Trivy against the built image.
starlette==1.3.1
uvicorn[standard]==0.34.0
# >=0.0.31 fixes CVE-2026-53537/53538/53539/53540 (parameter smuggling and
# related multipart parsing flaws). This parser sits directly under the /scan
# upload endpoint, so the fix is in the request path, not a transitive extra.
python-multipart==0.0.31

# Configuration and environment
python-dotenv==1.2.2  # CVE-2026-28684
pydantic==2.10.6
pydantic-settings==2.7.1

# Security
bcrypt==4.1.2

# Async operations
aiofiles==23.2.1

# Redis for caching and rate limiting
redis[hiredis]==5.0.1

# Database (PostgreSQL)
sqlalchemy==2.0.23
asyncpg==0.29.0
alembic==1.13.1

# Rate limiting
slowapi==0.1.9

# Monitoring and metrics
prometheus-client==0.19.0

